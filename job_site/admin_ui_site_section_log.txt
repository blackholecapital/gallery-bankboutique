[JOB-GALLERY-ADMIN-FLOW-V1 | WORKER A | S3 | RESULT]
status: PASS

scope:
- Implemented top /admin site-level editing section only (business/store + CTA/config basics + packageCardLimit + schema-backed load/save).

repo_changes:
- src/pages/admin/AdminPage.tsx

implemented_fields_v1:
- siteConfig: siteName, brandName, heroTitle, tagline, customDomain, contactEmail, layoutMode, packageTier, packageCardLimit, active
- commerceConfig: provider, mode, currency, sourceApp, successUrlTemplate, cancelUrlTemplate, usdc.receiveAddress

wiring:
- load path: useAdminDataset() -> loadAdminDataset()
- save path: save(dataset, 'admin-ui-site-section') -> saveAdminDataset()
- validation feedback: inline display of top validation errors when save fails

notes:
- Kept access gate behavior via VITE_ADMIN_CODE.
- Did not change schema/data-layer implementation; consumed existing S2 interfaces.
